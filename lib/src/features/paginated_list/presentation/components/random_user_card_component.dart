import 'package:flutter/material.dart';

class RandomUserCard extends StatelessWidget {
  const RandomUserCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card();
  }
}
